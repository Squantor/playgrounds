# Mregel string class
Mregel is a small string class that handles formatting adding etc.
## build instructions
To build: ```make```

To cleanup: ```make clean```

To execute ```make run```

Clang-tidy ```make check```

