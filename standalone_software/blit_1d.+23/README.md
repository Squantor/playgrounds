# Test program for 1D bitblits
This is a simple testing program for investigating various API's for 1D bit blits
## build instructions
To build: ```make```
To cleanup: ```make clean```
To execute ```make run```
To run clang-tidy ```make check```
Tests have a separate makefile that can be used with ```-ftests.mak```.
