# User interface class test
This is a simple tryout to see how I can make a embedded system user interface displaying on a graphical display.
## build instructions
To build: ```make```
To cleanup: ```make clean```
To execute ```make run```
Tests have their own makefile, run the above commands but add ```-f tests.mak```
