# print bitmap data
Paste in some data from a .xbm file so it is converted to something uint32 hexy.
## build commands
To build: ```make```
To cleanup: ```make clean```
To check with clang-tidy: ```make check```
To execute: ```make run```
